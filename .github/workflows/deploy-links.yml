# Deploy links app to Cloudflare Pages
name: Deploy links app

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'apps/links/**'
      - 'packages/**'
      - '.github/workflows/deploy-links.yml'

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build links app
        run: npm run build --workspace=apps/links

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy apps/links/dist --project-name=portfolio-links
