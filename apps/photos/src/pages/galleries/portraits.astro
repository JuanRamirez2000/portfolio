---
import GalleryLayout from '../../layouts/GalleryLayout.astro';
import { getPhotos, getHero } from '../../lib/api';

const [photos, heroPhotoId] = await Promise.all([getPhotos('portraits'), getHero('portraits')]);
---

<GalleryLayout
  title="Portraits — juanr.photo"
  galleryName="Portraits"
  tagline="faces, moods, and the occasional glare"
  heroLabel="portraits hero image"
  heroPhotoId={heroPhotoId}
  photos={photos}
/>
